begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_core.h>
end_include

begin_include
include|#
directive|include
file|<ngx_errno.h>
end_include

begin_include
include|#
directive|include
file|<ngx_log.h>
end_include

begin_include
include|#
directive|include
file|<ngx_connection.h>
end_include

begin_include
include|#
directive|include
file|<ngx_event.h>
end_include

begin_function
DECL|function|ngx_event_wsarecv (ngx_connection_t * c,char * buf,size_t size)
name|ssize_t
name|ngx_event_wsarecv
parameter_list|(
name|ngx_connection_t
modifier|*
name|c
parameter_list|,
name|char
modifier|*
name|buf
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
name|int
name|rc
decl_stmt|;
name|u_int
name|flags
decl_stmt|;
name|size_t
name|bytes
decl_stmt|;
name|ngx_err_t
name|err
decl_stmt|;
name|WSABUF
name|wsabuf
index|[
literal|1
index|]
decl_stmt|;
name|ngx_event_t
modifier|*
name|ev
decl_stmt|;
name|LPWSAOVERLAPPED_COMPLETION_ROUTINE
name|handler
decl_stmt|;
name|ev
operator|=
name|c
operator|->
name|read
expr_stmt|;
comment|/* DEBUG */
name|bytes
operator|=
literal|0
expr_stmt|;
if|if
condition|(
name|ev
operator|->
name|timedout
condition|)
block|{
name|ngx_set_socket_errno
argument_list|(
name|NGX_ETIMEDOUT
argument_list|)
expr_stmt|;
name|ngx_log_error
argument_list|(
name|NGX_LOG_ERR
argument_list|,
name|ev
operator|->
name|log
argument_list|,
literal|0
argument_list|,
literal|"WSARecv() timed out"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
if|if
condition|(
name|ev
operator|->
name|ready
condition|)
block|{
name|ev
operator|->
name|ready
operator|=
literal|0
expr_stmt|;
if|#
directive|if
operator|(
name|HAVE_IOCP_EVENT
operator|)
comment|/* iocp */
if|if
condition|(
name|ngx_event_flags
operator|&
name|NGX_HAVE_IOCP_EVENT
condition|)
block|{
if|if
condition|(
name|ev
operator|->
name|ovlp
operator|.
name|error
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ERR
argument_list|,
name|c
operator|->
name|log
argument_list|,
name|ev
operator|->
name|ovlp
operator|.
name|error
argument_list|,
literal|"WSARecv() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
return|return
name|ev
operator|->
name|available
return|;
block|}
endif|#
directive|endif
if|if
condition|(
name|WSAGetOverlappedResult
argument_list|(
name|c
operator|->
name|fd
argument_list|,
operator|(
name|LPWSAOVERLAPPED
operator|)
operator|&
name|ev
operator|->
name|ovlp
argument_list|,
operator|&
name|bytes
argument_list|,
literal|0
argument_list|,
name|NULL
argument_list|)
operator|==
literal|0
condition|)
block|{
name|err
operator|=
name|ngx_socket_errno
expr_stmt|;
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|err
argument_list|,
literal|"WSARecv() or WSAGetOverlappedResult() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
return|return
name|bytes
return|;
block|}
name|ngx_memzero
argument_list|(
operator|&
name|ev
operator|->
name|ovlp
argument_list|,
sizeof|sizeof
argument_list|(
name|WSAOVERLAPPED
argument_list|)
argument_list|)
expr_stmt|;
name|wsabuf
index|[
literal|0
index|]
operator|.
name|buf
operator|=
name|buf
expr_stmt|;
name|wsabuf
index|[
literal|0
index|]
operator|.
name|len
operator|=
name|size
expr_stmt|;
name|flags
operator|=
literal|0
expr_stmt|;
if|#
directive|if
literal|0
block_content|handler = ev->handler;
else|#
directive|else
name|handler
operator|=
name|NULL
expr_stmt|;
endif|#
directive|endif
name|rc
operator|=
name|WSARecv
argument_list|(
name|c
operator|->
name|fd
argument_list|,
name|wsabuf
argument_list|,
literal|1
argument_list|,
operator|&
name|bytes
argument_list|,
operator|&
name|flags
argument_list|,
operator|(
name|LPWSAOVERLAPPED
operator|)
operator|&
name|ev
operator|->
name|ovlp
argument_list|,
name|handler
argument_list|)
expr_stmt|;
name|ngx_log_debug
argument_list|(
argument|ev->log
argument_list|,
literal|"WSARecv: %d:%d"
argument|_ rc _ bytes
argument_list|)
empty_stmt|;
if|if
condition|(
name|rc
operator|==
operator|-
literal|1
condition|)
block|{
name|err
operator|=
name|ngx_socket_errno
expr_stmt|;
if|if
condition|(
name|err
operator|==
name|WSA_IO_PENDING
condition|)
block|{
return|return
name|NGX_AGAIN
return|;
block|}
else|else
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|err
argument_list|,
literal|"WSARecv() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
block|}
if|#
directive|if
operator|(
name|HAVE_IOCP_EVENT
operator|)
comment|/* iocp */
if|if
condition|(
name|ngx_event_flags
operator|&
name|NGX_HAVE_IOCP_EVENT
condition|)
block|{
return|return
name|NGX_AGAIN
return|;
block|}
endif|#
directive|endif
return|return
name|bytes
return|;
block|}
end_function

end_unit

