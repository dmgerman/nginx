begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_core.h>
end_include

begin_include
include|#
directive|include
file|<ngx_errno.h>
end_include

begin_include
include|#
directive|include
file|<ngx_log.h>
end_include

begin_include
include|#
directive|include
file|<ngx_recv.h>
end_include

begin_include
include|#
directive|include
file|<ngx_connection.h>
end_include

begin_include
include|#
directive|include
file|<ngx_event.h>
end_include

begin_if
if|#
directive|if
operator|(
name|HAVE_KQUEUE
operator|)
end_if

begin_include
include|#
directive|include
file|<ngx_kqueue_module.h>
end_include

begin_endif
endif|#
directive|endif
end_endif

begin_comment
comment|/*     The data is ready - 3 syscalls:         aio_write(), aio_error(), aio_return()     The data is not ready - 4 (kqueue) or 5 syscalls:         aio_write(), aio_error(), notifiction,                                              aio_error(), aio_return()                                              aio_cancel(), aio_error() */
end_comment

begin_function
DECL|function|ngx_event_aio_write (ngx_connection_t * c,char * buf,size_t size)
name|ssize_t
name|ngx_event_aio_write
parameter_list|(
name|ngx_connection_t
modifier|*
name|c
parameter_list|,
name|char
modifier|*
name|buf
parameter_list|,
name|size_t
name|size
parameter_list|)
block|{
name|int
name|rc
decl_stmt|,
name|first
decl_stmt|,
name|canceled
decl_stmt|;
name|ngx_event_t
modifier|*
name|ev
decl_stmt|;
name|ev
operator|=
name|c
operator|->
name|write
expr_stmt|;
name|canceled
operator|=
literal|0
expr_stmt|;
name|ngx_log_debug
argument_list|(
argument|ev->log
argument_list|,
literal|"aio: ev->ready: %d"
argument|_ ev->ready
argument_list|)
empty_stmt|;
name|ngx_log_debug
argument_list|(
name|ev
operator|->
name|log
argument_list|,
literal|"aio: aiocb: %08x"
name|_
operator|&
name|ev
operator|->
name|aiocb
argument_list|)
expr_stmt|;
if|if
condition|(
name|ev
operator|->
name|timedout
condition|)
block|{
name|ngx_set_socket_errno
argument_list|(
name|NGX_ETIMEDOUT
argument_list|)
expr_stmt|;
name|ngx_log_error
argument_list|(
name|NGX_LOG_ERR
argument_list|,
name|ev
operator|->
name|log
argument_list|,
literal|0
argument_list|,
literal|"aio_write() timed out"
argument_list|)
expr_stmt|;
name|rc
operator|=
name|aio_cancel
argument_list|(
name|c
operator|->
name|fd
argument_list|,
operator|&
name|ev
operator|->
name|aiocb
argument_list|)
expr_stmt|;
if|if
condition|(
name|rc
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"aio_cancel() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_log_debug
argument_list|(
argument|ev->log
argument_list|,
literal|"aio_cancel: %d"
argument|_ rc
argument_list|)
empty_stmt|;
name|canceled
operator|=
literal|1
expr_stmt|;
name|ev
operator|->
name|ready
operator|=
literal|1
expr_stmt|;
block|}
name|first
operator|=
literal|0
expr_stmt|;
if|if
condition|(
operator|!
name|ev
operator|->
name|ready
condition|)
block|{
name|ngx_memzero
argument_list|(
operator|&
name|ev
operator|->
name|aiocb
argument_list|,
sizeof|sizeof
argument_list|(
expr|struct
name|aiocb
argument_list|)
argument_list|)
expr_stmt|;
name|ev
operator|->
name|aiocb
operator|.
name|aio_fildes
operator|=
name|c
operator|->
name|fd
expr_stmt|;
name|ev
operator|->
name|aiocb
operator|.
name|aio_buf
operator|=
name|buf
expr_stmt|;
name|ev
operator|->
name|aiocb
operator|.
name|aio_nbytes
operator|=
name|size
expr_stmt|;
if|#
directive|if
operator|(
name|HAVE_KQUEUE
operator|)
name|ev
operator|->
name|aiocb
operator|.
name|aio_sigevent
operator|.
name|sigev_notify_kqueue
operator|=
name|kq
expr_stmt|;
name|ev
operator|->
name|aiocb
operator|.
name|aio_sigevent
operator|.
name|sigev_notify
operator|=
name|SIGEV_KEVENT
expr_stmt|;
name|ev
operator|->
name|aiocb
operator|.
name|aio_sigevent
operator|.
name|sigev_value
operator|.
name|sigval_ptr
operator|=
name|ev
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|aio_write
argument_list|(
operator|&
name|ev
operator|->
name|aiocb
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"aio_write() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_log_debug
argument_list|(
name|ev
operator|->
name|log
argument_list|,
literal|"aio_write: OK"
argument_list|)
expr_stmt|;
name|ev
operator|->
name|active
operator|=
literal|1
expr_stmt|;
name|first
operator|=
literal|1
expr_stmt|;
block|}
name|ev
operator|->
name|ready
operator|=
literal|0
expr_stmt|;
name|rc
operator|=
name|aio_error
argument_list|(
operator|&
name|ev
operator|->
name|aiocb
argument_list|)
expr_stmt|;
if|if
condition|(
name|rc
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"aio_error() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
if|if
condition|(
name|rc
operator|!=
literal|0
condition|)
block|{
if|if
condition|(
name|rc
operator|==
name|NGX_EINPROGRESS
condition|)
block|{
if|if
condition|(
operator|!
name|first
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|rc
argument_list|,
literal|"aio_write() still in progress"
argument_list|)
expr_stmt|;
block|}
return|return
name|NGX_AGAIN
return|;
block|}
if|if
condition|(
name|rc
operator|==
name|NGX_ECANCELED
operator|&&
name|canceled
condition|)
block|{
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|rc
argument_list|,
literal|"aio_write() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|rc
operator|=
name|aio_return
argument_list|(
operator|&
name|ev
operator|->
name|aiocb
argument_list|)
expr_stmt|;
if|if
condition|(
name|rc
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ev
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"aio_return() failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_log_debug
argument_list|(
argument|ev->log
argument_list|,
literal|"aio_write: %d"
argument|_ rc
argument_list|)
empty_stmt|;
return|return
name|rc
return|;
block|}
end_function

end_unit

