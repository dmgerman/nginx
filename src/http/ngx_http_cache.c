begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_core.h>
end_include

begin_include
include|#
directive|include
file|<ngx_http.h>
end_include

begin_include
include|#
directive|include
file|<md5.h>
end_include

begin_function
DECL|function|ngx_http_cache_get_file (ngx_http_request_t * r,ngx_http_cache_ctx_t * ctx)
name|int
name|ngx_http_cache_get_file
parameter_list|(
name|ngx_http_request_t
modifier|*
name|r
parameter_list|,
name|ngx_http_cache_ctx_t
modifier|*
name|ctx
parameter_list|)
block|{
name|MD5_CTX
name|md5
decl_stmt|;
name|ctx
operator|->
name|header_size
operator|=
sizeof|sizeof
argument_list|(
name|ngx_http_cache_header_t
argument_list|)
operator|+
name|ctx
operator|->
name|key
operator|.
name|len
operator|+
literal|1
expr_stmt|;
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|len
operator|=
name|ctx
operator|->
name|path
operator|->
name|name
operator|.
name|len
operator|+
literal|1
operator|+
name|ctx
operator|->
name|path
operator|->
name|len
operator|+
literal|32
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
operator|=
name|ngx_palloc
argument_list|(
name|r
operator|->
name|pool
argument_list|,
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|len
operator|+
literal|1
argument_list|)
operator|)
condition|)
block|{
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_memcpy
argument_list|(
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|,
name|ctx
operator|->
name|path
operator|->
name|name
operator|.
name|data
argument_list|,
name|ctx
operator|->
name|path
operator|->
name|name
operator|.
name|len
argument_list|)
expr_stmt|;
name|MD5Init
argument_list|(
operator|&
name|md5
argument_list|)
expr_stmt|;
name|MD5Update
argument_list|(
operator|&
name|md5
argument_list|,
operator|(
name|u_char
operator|*
operator|)
name|ctx
operator|->
name|key
operator|.
name|data
argument_list|,
name|ctx
operator|->
name|key
operator|.
name|len
argument_list|)
expr_stmt|;
name|MD5Final
argument_list|(
name|ctx
operator|->
name|md5
argument_list|,
operator|&
name|md5
argument_list|)
expr_stmt|;
name|ngx_md5_text
argument_list|(
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
operator|+
name|ctx
operator|->
name|path
operator|->
name|name
operator|.
name|len
operator|+
literal|1
operator|+
name|ctx
operator|->
name|path
operator|->
name|len
argument_list|,
name|ctx
operator|->
name|md5
argument_list|)
expr_stmt|;
name|ngx_log_debug
argument_list|(
argument|r->connection->log
argument_list|,
literal|"URL: %s, md5: %s"
argument|_ ctx->key.data _               ctx->file.name.data + ctx->path->name.len +
literal|1
argument|+ ctx->path->len
argument_list|)
empty_stmt|;
name|ngx_create_hashed_filename
argument_list|(
operator|&
name|ctx
operator|->
name|file
argument_list|,
name|ctx
operator|->
name|path
argument_list|)
expr_stmt|;
name|ngx_log_debug
argument_list|(
argument|r->connection->log
argument_list|,
literal|"FILE: %s"
argument|_ ctx->file.name.data
argument_list|)
empty_stmt|;
comment|/* TODO: look open files cache */
return|return
name|ngx_http_cache_open_file
argument_list|(
name|r
argument_list|,
name|ctx
argument_list|,
literal|0
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/* TODO: Win32 inode analogy */
end_comment

begin_function
DECL|function|ngx_http_cache_open_file (ngx_http_request_t * r,ngx_http_cache_ctx_t * ctx,ngx_file_uniq_t uniq)
name|int
name|ngx_http_cache_open_file
parameter_list|(
name|ngx_http_request_t
modifier|*
name|r
parameter_list|,
name|ngx_http_cache_ctx_t
modifier|*
name|ctx
parameter_list|,
name|ngx_file_uniq_t
name|uniq
parameter_list|)
block|{
name|ssize_t
name|n
decl_stmt|;
name|ngx_err_t
name|err
decl_stmt|;
name|ngx_http_cache_header_t
modifier|*
name|h
decl_stmt|;
name|ctx
operator|->
name|file
operator|.
name|fd
operator|=
name|ngx_open_file
argument_list|(
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|,
name|NGX_FILE_RDONLY
argument_list|,
name|NGX_FILE_OPEN
argument_list|)
expr_stmt|;
if|if
condition|(
name|ctx
operator|->
name|file
operator|.
name|fd
operator|==
name|NGX_INVALID_FILE
condition|)
block|{
name|err
operator|=
name|ngx_errno
expr_stmt|;
if|if
condition|(
name|err
operator|==
name|NGX_ENOENT
operator|||
name|err
operator|==
name|NGX_ENOTDIR
condition|)
block|{
return|return
name|NGX_DECLINED
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_open_file_n
literal|" \"%s\" failed"
argument_list|,
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
if|if
condition|(
name|uniq
condition|)
block|{
if|if
condition|(
name|ngx_stat_fd
argument_list|(
name|ctx
operator|->
name|file
operator|.
name|fd
argument_list|,
operator|&
name|ctx
operator|->
name|file
operator|.
name|info
argument_list|)
operator|==
name|NGX_FILE_ERROR
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_stat_fd_n
literal|" \"%s\" failed"
argument_list|,
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
if|if
condition|(
name|ngx_file_uniq
argument_list|(
name|ctx
operator|->
name|file
operator|.
name|info
argument_list|)
operator|==
name|uniq
condition|)
block|{
if|if
condition|(
name|ngx_close_file
argument_list|(
name|ctx
operator|->
name|file
operator|.
name|fd
argument_list|)
operator|==
name|NGX_FILE_ERROR
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_close_file_n
literal|" \"%s\" failed"
argument_list|,
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|)
expr_stmt|;
block|}
return|return
name|NGX_HTTP_CACHE_THE_SAME
return|;
block|}
block|}
name|n
operator|=
name|ngx_read_file
argument_list|(
operator|&
name|ctx
operator|->
name|file
argument_list|,
name|ctx
operator|->
name|buf
operator|->
name|pos
argument_list|,
name|ctx
operator|->
name|buf
operator|->
name|end
operator|-
name|ctx
operator|->
name|buf
operator|->
name|last
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|n
operator|==
name|NGX_ERROR
operator|||
name|n
operator|==
name|NGX_AGAIN
condition|)
block|{
return|return
name|n
return|;
block|}
if|if
condition|(
name|n
operator|<=
name|ctx
operator|->
name|header_size
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
literal|0
argument_list|,
literal|"cache file \"%s\" is too small"
argument_list|,
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|h
operator|=
operator|(
name|ngx_http_cache_header_t
operator|*
operator|)
name|ctx
operator|->
name|buf
operator|->
name|pos
expr_stmt|;
name|ctx
operator|->
name|expires
operator|=
name|h
operator|->
name|expires
expr_stmt|;
name|ctx
operator|->
name|last_modified
operator|=
name|h
operator|->
name|last_modified
expr_stmt|;
name|ctx
operator|->
name|date
operator|=
name|h
operator|->
name|date
expr_stmt|;
name|ctx
operator|->
name|length
operator|=
name|h
operator|->
name|length
expr_stmt|;
if|if
condition|(
name|h
operator|->
name|key_len
operator|!=
name|ctx
operator|->
name|key
operator|.
name|len
operator|||
name|ngx_strncmp
argument_list|(
name|h
operator|->
name|key
argument_list|,
name|ctx
operator|->
name|key
operator|.
name|data
argument_list|,
name|h
operator|->
name|key_len
argument_list|)
operator|!=
literal|0
condition|)
block|{
name|h
operator|->
name|key
index|[
name|h
operator|->
name|key_len
index|]
operator|=
literal|'\0'
expr_stmt|;
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
literal|0
argument_list|,
literal|"md5 collision: \"%s\" and \"%s\""
argument_list|,
name|h
operator|->
name|key
argument_list|,
name|ctx
operator|->
name|key
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_DECLINED
return|;
block|}
name|ctx
operator|->
name|buf
operator|->
name|last
operator|+=
name|n
expr_stmt|;
if|if
condition|(
name|ctx
operator|->
name|expires
operator|<
name|ngx_time
argument_list|()
condition|)
block|{
name|ngx_log_debug
argument_list|(
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
literal|"EXPIRED"
argument_list|)
expr_stmt|;
return|return
name|NGX_HTTP_CACHE_STALE
return|;
block|}
comment|/* TODO: NGX_HTTP_CACHE_AGED */
return|return
name|NGX_OK
return|;
block|}
end_function

begin_function
DECL|function|ngx_http_cache_update_file (ngx_http_request_t * r,ngx_http_cache_ctx_t * ctx,ngx_str_t * temp_file)
name|int
name|ngx_http_cache_update_file
parameter_list|(
name|ngx_http_request_t
modifier|*
name|r
parameter_list|,
name|ngx_http_cache_ctx_t
modifier|*
name|ctx
parameter_list|,
name|ngx_str_t
modifier|*
name|temp_file
parameter_list|)
block|{
name|int
name|retry
decl_stmt|;
name|ngx_err_t
name|err
decl_stmt|;
name|retry
operator|=
literal|0
expr_stmt|;
for|for
control|(
init|;
condition|;
control|)
block|{
if|if
condition|(
name|ngx_rename_file
argument_list|(
name|temp_file
operator|->
name|data
argument_list|,
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|)
operator|!=
name|NGX_FILE_ERROR
condition|)
block|{
return|return
name|NGX_OK
return|;
block|}
name|err
operator|=
name|ngx_errno
expr_stmt|;
if|if
condition|(
name|retry
operator|||
operator|(
name|err
operator|!=
name|NGX_ENOENT
operator|&&
name|err
operator|!=
name|NGX_ENOTDIR
operator|)
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_rename_file_n
literal|"(\"%s\", \"%s\") failed"
argument_list|,
name|temp_file
operator|->
name|data
argument_list|,
name|ctx
operator|->
name|file
operator|.
name|name
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
if|if
condition|(
name|ngx_create_path
argument_list|(
operator|&
name|ctx
operator|->
name|file
argument_list|,
name|ctx
operator|->
name|path
argument_list|)
operator|==
name|NGX_ERROR
condition|)
block|{
return|return
name|NGX_ERROR
return|;
block|}
name|retry
operator|=
literal|1
expr_stmt|;
block|}
block|}
end_function

begin_if
if|#
directive|if
literal|0
end_if

begin_comment
comment|/*  * small file in malloc()ed memory, mmap()ed file, file descriptor only,  * file access time only (to estimate could pages still be in memory),  * translated URI (ngx_http_index_hanlder),  * compiled script (ngx_http_ssi_filter).  */
end_comment

begin_define
define|#
directive|define
name|NGX_HTTP_CACHE_ENTRY_DELETED
value|0x00000001
end_define

begin_define
define|#
directive|define
name|NGX_HTTP_CACHE_ENTRY_MMAPED
value|0x00000002
end_define

begin_comment
comment|/* "/" -> "/index.html" in ngx_http_index_handler */
end_comment

begin_define
define|#
directive|define
name|NGX_HTTP_CACHE_ENTRY_URI
value|0x00000004
end_define

begin_comment
comment|/* 301 location "/dir" -> "dir/" in ngx_http_core_handler */
end_comment

begin_comment
comment|/* compiled script in ngx_http_ssi_filter  */
end_comment

begin_define
define|#
directive|define
name|NGX_HTTP_CACHE_ENTRY_SCRIPT
value|0x00000008
end_define

begin_define
define|#
directive|define
name|NGX_HTTP_CACHE_FILTER_FLAGS
value|0xFFFF0000
end_define

begin_comment
unit|typedef struct {     ngx_fd_t   fd;     off_t      size;     void      *data;     time_t     accessed;     time_t     last_modified;     time_t     updated;
comment|/* no needed with kqueue */
end_comment

begin_comment
unit|int        refs;     int        flags; } ngx_http_cache_entry_t;   typedef struct {     u_int32_t          crc;     ngx_str_t          uri;     ngx_http_cache_t  *cache; } ngx_http_cache_hash_entry_t;   typedef struct {     ngx_http_cache_t  *cache;     u_int32_t          crc;     int                n; } ngx_http_cache_handle_t;    int ngx_http_cache_get(ngx_http_cache_hash_t *cache_hash,                        ngx_str_t *uri, ngx_http_cache_handle_t *h) {     int                           hi;     ngx_http_cache_hash_entry_t  *entry;      h->crc = ngx_crc(uri->data, uri->len);      hi = h->crc % cache_hash->size;     entry = cache_hash[hi].elts;      for (i = 0; i< cache_hash[hi].nelts; i++) {         if (entry[i].crc == crc&& entry[i].uri.len == uri->len&& ngx_strncmp(entry[i].uri.data, uri->data, uri->len) == 0         {             h->cache = entry[i].cache;             h->cache->refs++;             h->n = hi;             return NGX_OK;         }     }      return NGX_ERROR; }
comment|/* 32-bit crc16 */
end_comment

begin_comment
unit|int ngx_crc(char *data, size_t len) {     u_int32_t  sum;      for (sum = 0; len; len--) {
comment|/*          * gcc 2.95.2 x86 and icc 7.1.006 compile that operator          * into the single rol opcode.          * msvc 6.0sp2 compiles it into four opcodes.          */
end_comment

begin_endif
unit|sum = sum>> 1 | sum<< 31;          sum += *data++;     }      return sum; }
endif|#
directive|endif
end_endif

end_unit

