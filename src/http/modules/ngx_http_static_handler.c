begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_strings.h>
end_include

begin_include
include|#
directive|include
file|<ngx_open.h>
end_include

begin_include
include|#
directive|include
file|<ngx_stat.h>
end_include

begin_include
include|#
directive|include
file|<ngx_http.h>
end_include

begin_function
DECL|function|ngx_http_static_handler (ngx_http_request_t * r)
name|int
name|ngx_http_static_handler
parameter_list|(
name|ngx_http_request_t
modifier|*
name|r
parameter_list|)
block|{
name|int
name|index_len
decl_stmt|,
name|err
decl_stmt|,
name|i
decl_stmt|;
name|char
modifier|*
name|name
decl_stmt|,
modifier|*
name|loc
decl_stmt|,
modifier|*
name|file
name|ngx_file_t
name|fd
decl_stmt|;
name|ngx_http_header_out_t
name|out
decl_stmt|;
name|ngx_http_event_static_handler_loc_conf_t
modifier|*
name|cf
decl_stmt|;
name|cf
operator|=
operator|(
name|ngx_http_event_static_handler_loc_conf_t
operator|*
operator|)
name|ngx_get_module_loc_conf
argument_list|(
name|r
argument_list|,
operator|&
name|ngx_http_event_static_handler_module
argument_list|)
expr_stmt|;
name|ngx_assert
argument_list|(
argument|r->fd
argument_list|,
argument|return NGX_HTTP_INTERNAL_SERVER_ERROR
argument_list|,
argument|r->connection->log
argument_list|,
literal|"ngx_http_static_handler: no file"
argument_list|)
empty_stmt|;
name|out
operator|.
name|status
operator|=
name|NGX_HTTP_OK
expr_stmt|;
name|out
operator|.
name|content_length
operator|=
name|r
operator|->
name|stat
operator|.
name|sb_size
expr_stmt|;
name|out
operator|.
name|last_modified
operator|=
name|r
operator|->
name|stat
operator|.
name|sb_mtime
expr_stmt|;
comment|/* */
name|out
operator|.
name|content_type
operator|=
literal|"text/html"
expr_stmt|;
name|rc
operator|=
name|ngx_send_http_header
argument_list|(
operator|&
name|out
argument_list|)
expr_stmt|;
if|if
condition|(
name|r
operator|->
name|header_only
condition|)
return|return
name|rc
return|;
comment|/* NGX_HTTP_INTERNAL_SERVER_ERROR is too late */
name|ngx_test_null
argument_list|(
name|h
argument_list|,
name|ngx_create_hunk
argument_list|(
name|r
operator|->
name|pool
argument_list|)
argument_list|,
name|NGX_HTTP_INTERNAL_SERVER_ERROR
argument_list|)
expr_stmt|;
name|h
operator|->
name|type
operator|=
name|NGX_HUNK_FILE
operator||
name|NGX_HUNK_LAST
expr_stmt|;
name|h
operator|->
name|fd
operator|=
name|r
operator|->
name|fd
expr_stmt|;
name|h
operator|->
name|pos
operator|.
name|file
operator|=
literal|0
expr_stmt|;
name|h
operator|->
name|end
operator|.
name|file
operator|=
name|r
operator|->
name|stat
operator|.
name|sb_size
expr_stmt|;
name|ngx_test_null
argument_list|(
name|ch
argument_list|,
name|ngx_create_chain
argument_list|(
name|r
operator|->
name|pool
argument_list|)
argument_list|,
name|NGX_HTTP_INTERNAL_SERVER_ERROR
argument_list|)
expr_stmt|;
name|ch
operator|->
name|hunk
operator|=
name|h
expr_stmt|;
name|ch
operator|->
name|next
operator|=
name|NULL
expr_stmt|;
return|return
name|ngx_http_filter
argument_list|(
name|ch
argument_list|)
return|;
block|}
end_function

begin_comment
comment|/*  static void *ngx_create_index_config() {     ngx_http_index_handler_loc_conf_t  *cf;      ngx_check_null(cf, ngx_alloc(p, sizeof(ngx_http_index_handler_loc_conf)),                    NULL);      cf->indices = ngx_create_array(p, sizeof(ngx_http_index_t), 5);     if (cf->indices == NULL)         return NULL;      cf->max_index_len = 0;      return cf; }  static void *ngx_merge_index_config() {     if (p->indices->nelts> 0) {          copy and check dups          if (c->max_index_len< c->max_index_len)             c->max_index_len< c->max_index_len);     } }  static void *ngx_set_index() {     if (*conf == NULL) {         cf = ngx_create_index_conf();         if (cf == NULL)             return "can not create config";     }      while (args) {        index = ngx_push_array(cf->indices);        index->name = arg;        index->len = ngx_strlen(arg) + 1;         if (cf->max_index_len< index->len)            cf->max_index_len = index->len;     }      *conf = cf; }  */
end_comment

end_unit

