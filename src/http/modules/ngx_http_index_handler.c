begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_strings.h>
end_include

begin_include
include|#
directive|include
file|<ngx_open.h>
end_include

begin_include
include|#
directive|include
file|<ngx_stat.h>
end_include

begin_include
include|#
directive|include
file|<ngx_http.h>
end_include

begin_function
DECL|function|ngx_http_index_handler (ngx_http_request_t * r)
name|int
name|ngx_http_index_handler
parameter_list|(
name|ngx_http_request_t
modifier|*
name|r
parameter_list|)
block|{
name|int
name|index_len
decl_stmt|,
name|err
decl_stmt|,
name|i
decl_stmt|;
name|char
modifier|*
name|name
decl_stmt|,
modifier|*
name|loc
decl_stmt|,
modifier|*
name|file
name|ngx_file_t
name|fd
decl_stmt|;
name|ngx_http_index_t
modifier|*
name|index
decl_stmt|;
name|ngx_http_index_handler_loc_conf_t
modifier|*
name|cf
decl_stmt|;
name|cf
operator|=
operator|(
name|ngx_http_index_handler_loc_conf_t
operator|*
operator|)
name|ngx_get_module_loc_conf
argument_list|(
name|r
argument_list|,
operator|&
name|ngx_http_index_handler_module
argument_list|)
expr_stmt|;
name|index_len
operator|=
operator|(
operator|*
operator|(
name|r
operator|->
name|uri_end
operator|-
literal|1
operator|)
operator|==
literal|'/'
operator|)
condition|?
name|cf
operator|->
name|max_index_len
else|:
literal|0
expr_stmt|;
name|ngx_test_null
argument_list|(
name|name
argument_list|,
name|ngx_palloc
argument_list|(
name|r
operator|->
name|pool
argument_list|,
name|r
operator|->
name|uri_end
operator|-
name|r
operator|->
name|uri_start
operator|+
name|index_len
operator|+
name|r
operator|->
name|server
operator|->
name|doc_root_len
argument_list|)
argument_list|,
name|NGX_HTTP_INTERNAL_SERVER_ERROR
argument_list|)
expr_stmt|;
name|loc
operator|=
name|ngx_cpystrn
argument_list|(
name|name
argument_list|,
name|r
operator|->
name|server
operator|->
name|doc_root
argument_list|,
name|r
operator|->
name|server
operator|->
name|doc_root_len
argument_list|)
expr_stmt|;
name|file
operator|=
name|ngx_cpystrn
argument_list|(
name|loc
argument_list|,
name|r
operator|->
name|uri_start
argument_list|,
name|r
operator|->
name|uri_end
operator|-
name|r
operator|->
name|uri_start
operator|+
literal|1
argument_list|)
expr_stmt|;
comment|/* URI without / on the end - check directory */
if|if
condition|(
name|index_len
operator|==
literal|0
condition|)
block|{
if|if
condition|(
name|ngx_stat
argument_list|(
name|name
argument_list|,
operator|&
name|r
operator|->
name|stat
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|err
operator|=
name|ngx_errno
expr_stmt|;
name|ngx_log_error
argument_list|(
name|GX_LOG_ERR
argument_list|,
name|r
operator|->
name|connection
operator|->
name|log
argument_list|,
name|err
argument_list|,
literal|"ngx_http_handler: "
name|ngx_stat_n
literal|" %s failed"
argument_list|,
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
operator|==
name|NGX_ENOENT
condition|)
return|return
name|NGX_HTTP_NOT_FOUND
return|;
else|else
return|return
name|NGX_HTTP_INTERNAL_SERVER_ERROR
return|;
block|}
if|if
condition|(
name|ngx_is_dir
argument_list|(
name|r
operator|->
name|stat
argument_list|)
condition|)
block|{
operator|*
name|file
operator|++
operator|=
literal|'/'
expr_stmt|;
operator|*
name|file
operator|=
literal|'\0'
expr_stmt|;
name|r
operator|->
name|headers_out
operator|->
name|location
operator|=
name|loc
expr_stmt|;
return|return
name|NGX_HTTP_MOVED_PERMANENTLY
return|;
block|}
name|r
operator|->
name|file
operator|=
name|name
expr_stmt|;
name|r
operator|->
name|stat_valid
operator|=
literal|1
expr_stmt|;
return|return
name|NGX_OK
return|;
block|}
comment|/* look for index file */
name|index
operator|=
operator|(
name|ngx_http_index_t
operator|*
operator|)
name|cf
operator|->
name|indices
operator|->
name|elts
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|cf
operator|->
name|indices
operator|->
name|nelts
condition|;
name|i
operator|++
control|)
block|{
name|ngx_memcpy
argument_list|(
argument|file
argument_list|,
argument|index[i].name; index[i].len
argument_list|)
empty_stmt|;
name|fd
operator|=
name|ngx_open
argument_list|(
name|name
argument_list|,
name|O_RDONLY
argument_list|)
expr_stmt|;
if|if
condition|(
name|fd
operator|!=
operator|-
literal|1
condition|)
block|{
name|r
operator|->
name|file
operator|=
name|name
expr_stmt|;
name|r
operator|->
name|fd
operator|=
name|fd
expr_stmt|;
return|return
name|NGX_OK
return|;
block|}
block|}
return|return
name|NGX_HTTP_FORBIDDEN
return|;
block|}
end_function

begin_comment
comment|/*  static void *ngx_create_index_config() {     ngx_http_index_handler_loc_conf_t  *cf;      ngx_check_null(cf, ngx_alloc(p, sizeof(ngx_http_index_handler_loc_conf)),                    NULL);      cf->indices = ngx_create_array(p, sizeof(ngx_http_index_t), 5);     if (cf->indices == NULL)         return NULL;      cf->max_index_len = 0;      return cf; }  static void *ngx_merge_index_config() {     if (p->indices->nelts> 0) {          copy and check dups          if (c->max_index_len< c->max_index_len)             c->max_index_len< c->max_index_len);     } }  static void *ngx_set_index() {     if (*conf == NULL) {         cf = ngx_create_index_conf();         if (cf == NULL)             return "can not create config";     }      while (args) {        index = ngx_push_array(cf->indices);        index->name = arg;        index->len = ngx_strlen(arg) + 1;         if (cf->max_index_len< index->len)            cf->max_index_len = index->len;     }      *conf = cf; }  */
end_comment

end_unit

