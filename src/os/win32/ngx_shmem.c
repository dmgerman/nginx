begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_comment
comment|/*  * Copyright (C) Igor Sysoev  * Copyright (C) Nginx, Inc.  */
end_comment

begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_core.h>
end_include

begin_comment
comment|/*  * Base addresses selected by system for shared memory mappings are likely  * to be different on Windows Vista and later versions due to address space  * layout randomization.  This is however incompatible with storing absolute  * addresses within the shared memory.  *  * To make it possible to store absolute addresses we create mappings  * at the same address in all processes by starting mappings at predefined  * addresses.  The addresses were selected somewhat randomly in order to  * minimize the probability that some other library doing something similar  * conflicts with us.  The addresses are from the following typically free  * blocks:  *  * - 0x10000000 .. 0x70000000 (about 1.5 GB in total) on 32-bit platforms  * - 0x000000007fff0000 .. 0x000007f68e8b0000 (about 8 TB) on 64-bit platforms  *  * Additionally, we allow to change the mapping address once it was detected  * to be different from one originally used.  This is needed to support  * reconfiguration.  */
end_comment

begin_ifdef
ifdef|#
directive|ifdef
name|_WIN64
end_ifdef

begin_define
DECL|macro|NGX_SHMEM_BASE
define|#
directive|define
name|NGX_SHMEM_BASE
value|0x0000047047e00000
end_define

begin_else
else|#
directive|else
end_else

begin_define
DECL|macro|NGX_SHMEM_BASE
define|#
directive|define
name|NGX_SHMEM_BASE
value|0x2efe0000
end_define

begin_endif
endif|#
directive|endif
end_endif

begin_decl_stmt
DECL|variable|ngx_allocation_granularity
name|ngx_uint_t
name|ngx_allocation_granularity
decl_stmt|;
end_decl_stmt

begin_function
name|ngx_int_t
DECL|function|ngx_shm_alloc (ngx_shm_t * shm)
name|ngx_shm_alloc
parameter_list|(
name|ngx_shm_t
modifier|*
name|shm
parameter_list|)
block|{
name|u_char
modifier|*
name|name
decl_stmt|;
name|uint64_t
name|size
decl_stmt|;
specifier|static
name|u_char
modifier|*
name|base
init|=
operator|(
name|u_char
operator|*
operator|)
name|NGX_SHMEM_BASE
decl_stmt|;
name|name
operator|=
name|ngx_alloc
argument_list|(
name|shm
operator|->
name|name
operator|.
name|len
operator|+
literal|2
operator|+
name|NGX_INT32_LEN
argument_list|,
name|shm
operator|->
name|log
argument_list|)
expr_stmt|;
if|if
condition|(
name|name
operator|==
name|NULL
condition|)
block|{
return|return
name|NGX_ERROR
return|;
block|}
operator|(
name|void
operator|)
name|ngx_sprintf
argument_list|(
name|name
argument_list|,
literal|"%V_%s%Z"
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|,
name|ngx_unique
argument_list|)
expr_stmt|;
name|ngx_set_errno
argument_list|(
literal|0
argument_list|)
expr_stmt|;
name|size
operator|=
name|shm
operator|->
name|size
expr_stmt|;
name|shm
operator|->
name|handle
operator|=
name|CreateFileMapping
argument_list|(
name|INVALID_HANDLE_VALUE
argument_list|,
name|NULL
argument_list|,
name|PAGE_READWRITE
argument_list|,
operator|(
name|u_long
operator|)
operator|(
name|size
operator|>>
literal|32
operator|)
argument_list|,
operator|(
name|u_long
operator|)
operator|(
name|size
operator|&
literal|0xffffffff
operator|)
argument_list|,
operator|(
name|char
operator|*
operator|)
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|shm
operator|->
name|handle
operator|==
name|NULL
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"CreateFileMapping(%uz, %s) failed"
argument_list|,
name|shm
operator|->
name|size
argument_list|,
name|name
argument_list|)
expr_stmt|;
name|ngx_free
argument_list|(
name|name
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_free
argument_list|(
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|ngx_errno
operator|==
name|ERROR_ALREADY_EXISTS
condition|)
block|{
name|shm
operator|->
name|exists
operator|=
literal|1
expr_stmt|;
block|}
name|shm
operator|->
name|addr
operator|=
name|MapViewOfFileEx
argument_list|(
name|shm
operator|->
name|handle
argument_list|,
name|FILE_MAP_WRITE
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
name|base
argument_list|)
expr_stmt|;
if|if
condition|(
name|shm
operator|->
name|addr
operator|!=
name|NULL
condition|)
block|{
name|base
operator|+=
name|ngx_align
argument_list|(
name|size
argument_list|,
name|ngx_allocation_granularity
argument_list|)
expr_stmt|;
return|return
name|NGX_OK
return|;
block|}
name|ngx_log_debug3
argument_list|(
name|NGX_LOG_DEBUG_CORE
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"MapViewOfFileEx(%uz, %p) of file mapping \"%V\" failed, "
literal|"retry without a base address"
argument_list|,
name|shm
operator|->
name|size
argument_list|,
name|base
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|)
expr_stmt|;
comment|/*      * Order of shared memory zones may be different in the master process      * and worker processes after reconfiguration.  As a result, the above      * may fail due to a conflict with a previously created mapping remapped      * to a different address.  Additionally, there may be a conflict with      * some other uses of the memory.  In this case we retry without a base      * address to let the system assign the address itself.      */
name|shm
operator|->
name|addr
operator|=
name|MapViewOfFile
argument_list|(
name|shm
operator|->
name|handle
argument_list|,
name|FILE_MAP_WRITE
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|)
expr_stmt|;
if|if
condition|(
name|shm
operator|->
name|addr
operator|!=
name|NULL
condition|)
block|{
return|return
name|NGX_OK
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"MapViewOfFile(%uz) of file mapping \"%V\" failed"
argument_list|,
name|shm
operator|->
name|size
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|)
expr_stmt|;
if|if
condition|(
name|CloseHandle
argument_list|(
name|shm
operator|->
name|handle
argument_list|)
operator|==
literal|0
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"CloseHandle() of file mapping \"%V\" failed"
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|)
expr_stmt|;
block|}
return|return
name|NGX_ERROR
return|;
block|}
end_function

begin_function
name|ngx_int_t
DECL|function|ngx_shm_remap (ngx_shm_t * shm,u_char * addr)
name|ngx_shm_remap
parameter_list|(
name|ngx_shm_t
modifier|*
name|shm
parameter_list|,
name|u_char
modifier|*
name|addr
parameter_list|)
block|{
if|if
condition|(
name|UnmapViewOfFile
argument_list|(
name|shm
operator|->
name|addr
argument_list|)
operator|==
literal|0
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"UnmapViewOfFile(%p) of file mapping \"%V\" failed"
argument_list|,
name|shm
operator|->
name|addr
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|shm
operator|->
name|addr
operator|=
name|MapViewOfFileEx
argument_list|(
name|shm
operator|->
name|handle
argument_list|,
name|FILE_MAP_WRITE
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
literal|0
argument_list|,
name|addr
argument_list|)
expr_stmt|;
if|if
condition|(
name|shm
operator|->
name|addr
operator|!=
name|NULL
condition|)
block|{
return|return
name|NGX_OK
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"MapViewOfFileEx(%uz, %p) of file mapping \"%V\" failed"
argument_list|,
name|shm
operator|->
name|size
argument_list|,
name|addr
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
end_function

begin_function
name|void
DECL|function|ngx_shm_free (ngx_shm_t * shm)
name|ngx_shm_free
parameter_list|(
name|ngx_shm_t
modifier|*
name|shm
parameter_list|)
block|{
if|if
condition|(
name|UnmapViewOfFile
argument_list|(
name|shm
operator|->
name|addr
argument_list|)
operator|==
literal|0
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"UnmapViewOfFile(%p) of file mapping \"%V\" failed"
argument_list|,
name|shm
operator|->
name|addr
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|CloseHandle
argument_list|(
name|shm
operator|->
name|handle
argument_list|)
operator|==
literal|0
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|shm
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"CloseHandle() of file mapping \"%V\" failed"
argument_list|,
operator|&
name|shm
operator|->
name|name
argument_list|)
expr_stmt|;
block|}
block|}
end_function

end_unit

