begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_freebsd_init.h>
end_include

begin_decl_stmt
DECL|variable|freebsd_kern_osreldate
name|int
name|freebsd_kern_osreldate
decl_stmt|;
end_decl_stmt

begin_decl_stmt
DECL|variable|freebsd_hw_ncpu
name|int
name|freebsd_hw_ncpu
decl_stmt|;
end_decl_stmt

begin_decl_stmt
DECL|variable|freebsd_sendfile_nbytes_bug
name|int
name|freebsd_sendfile_nbytes_bug
decl_stmt|;
end_decl_stmt

begin_function
DECL|function|ngx_os_init (ngx_log_t * log)
name|int
name|ngx_os_init
parameter_list|(
name|ngx_log_t
modifier|*
name|log
parameter_list|)
block|{
name|size_t
name|size
decl_stmt|;
name|size
operator|=
literal|4
expr_stmt|;
if|if
condition|(
name|sysctlbyname
argument_list|(
literal|"kern.osreldate"
argument_list|,
operator|&
name|freebsd_kern_osreldate
argument_list|,
operator|&
name|size
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|log
argument_list|,
name|errno
argument_list|,
literal|"sysctlbyname(kern.osreldate) failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_INFO
argument_list|,
name|log
argument_list|,
literal|0
argument_list|,
literal|"kern.osreldate: %d, built on %d"
argument_list|,
name|freebsd_kern_osreldate
argument_list|,
name|__FreeBSD_version
argument_list|)
expr_stmt|;
if|#
directive|if
name|HAVE_FREEBSD_SENDFILE
comment|/* The determination of the sendfile() nbytes bug is complex enough.        There're two sendfile() syscalls: a new 393 has no bug while        an old 336 has the bug in some versions and has not in others.        libc_r wrapper also emulates the bug in some versions.        There's no way to say exactly if a given FreeBSD version has bug.        Here is the algorithm that work at least for RELEASEs        and for syscalls only (not libc_r wrapper). */
comment|/* detect was the new sendfile() version available at the compile time        to allow an old binary to run correctly on an updated FreeBSD system. */
if|#
directive|if
operator|(
name|__FreeBSD__
operator|==
literal|4
operator|&&
name|__FreeBSD_version
operator|>=
literal|460102
operator|)
expr|\
operator|||
name|__FreeBSD_version
operator|==
literal|460002
operator|||
name|__FreeBSD_version
operator|>=
literal|500039
comment|/* a new syscall without the bug */
name|freebsd_sendfile_nbytes_bug
operator|=
literal|0
expr_stmt|;
else|#
directive|else
comment|/* an old syscall that can have the bug */
name|freebsd_sendfile_nbytes_bug
operator|=
literal|1
expr_stmt|;
endif|#
directive|endif
endif|#
directive|endif
comment|/* HAVE_FREEBSD_SENDFILE */
name|size
operator|=
literal|4
expr_stmt|;
if|if
condition|(
name|sysctlbyname
argument_list|(
literal|"hw.ncpu"
argument_list|,
operator|&
name|freebsd_hw_ncpu
argument_list|,
operator|&
name|size
argument_list|,
name|NULL
argument_list|,
literal|0
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ALERT
argument_list|,
name|log
argument_list|,
name|errno
argument_list|,
literal|"sysctlbyname(hw.ncpu) failed"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_INFO
argument_list|,
name|log
argument_list|,
literal|0
argument_list|,
literal|"hw.ncpu: %d"
argument_list|,
name|freebsd_hw_ncpu
argument_list|)
expr_stmt|;
return|return
name|NGX_OK
return|;
block|}
end_function

end_unit

