begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_core.h>
end_include

begin_typedef
DECL|typedef|ngx_gc_t
typedef|typedef
name|struct
name|ngx_gc_s
name|ngx_gc_t
typedef|;
end_typedef

begin_typedef
DECL|typedef|ngx_gc_handler_pt
typedef|typedef
name|int
function_decl|(
modifier|*
name|ngx_gc_handler_pt
function_decl|)
parameter_list|(
name|ngx_gc_t
modifier|*
name|ctx
parameter_list|,
name|ngx_str_t
modifier|*
name|name
parameter_list|,
name|ngx_file_info_t
modifier|*
name|fi
parameter_list|)
function_decl|;
end_typedef

begin_struct
DECL|struct|ngx_gc_s
struct|struct
name|ngx_gc_s
block|{
DECL|member|path
name|ngx_path_t
modifier|*
name|path
decl_stmt|;
DECL|member|deleted
name|u_int
name|deleted
decl_stmt|;
DECL|member|freed
name|off_t
name|freed
decl_stmt|;
DECL|member|handler
name|ngx_gc_handler_pt
name|handler
decl_stmt|;
DECL|member|log
name|ngx_log_t
modifier|*
name|log
decl_stmt|;
block|}
struct|;
end_struct

begin_function_decl
specifier|static
name|int
name|ngx_collect_garbage
parameter_list|(
name|ngx_gc_t
modifier|*
name|ctx
parameter_list|,
name|ngx_str_t
modifier|*
name|dname
parameter_list|,
name|int
name|level
parameter_list|)
function_decl|;
end_function_decl

begin_if
if|#
directive|if
literal|0
end_if

begin_endif
unit|{     ngx_test_null(cycle->timer_events,                   ngx_alloc(sizeof(ngx_event_t) * TIMERS, cycle->log),                   NGX_ERROR);      ngx_event_timer_init(cycle); }   void garbage_collector() {     ngx_msec_t        timer;     struct timeval    tv;     ngx_epoch_msec_t  delta;      for ( ;; ) {         timer = ngx_event_find_timer();          ngx_gettimeofday(&tv);         delta = tv.tv_sec * 1000 + tv.tv_usec / 1000;          msleep(timer);          ngx_gettimeofday(&tv);          ngx_cached_time = tv.tv_sec;         ngx_time_update();          delta = tv.tv_sec * 1000 + tv.tv_usec / 1000 - delta;          ngx_event_expire_timers((ngx_msec_t) delta);     } }
endif|#
directive|endif
end_endif

begin_function
DECL|function|stub_init (ngx_log_t * log)
name|void
name|stub_init
parameter_list|(
name|ngx_log_t
modifier|*
name|log
parameter_list|)
block|{
name|ngx_gc_t
modifier|*
name|ctx
decl_stmt|;
name|ngx_path_t
name|path
decl_stmt|;
if|if
condition|(
operator|!
operator|(
name|ctx
operator|=
name|ngx_alloc
argument_list|(
sizeof|sizeof
argument_list|(
name|ngx_gc_t
argument_list|)
argument_list|,
name|log
argument_list|)
operator|)
condition|)
block|{
return|return;
block|}
name|path
operator|.
name|name
operator|.
name|len
operator|=
literal|4
expr_stmt|;
name|path
operator|.
name|name
operator|.
name|data
operator|=
literal|"temp"
expr_stmt|;
name|path
operator|.
name|len
operator|=
literal|5
expr_stmt|;
name|path
operator|.
name|level
index|[
literal|0
index|]
operator|=
literal|1
expr_stmt|;
name|path
operator|.
name|level
index|[
literal|1
index|]
operator|=
literal|2
expr_stmt|;
name|path
operator|.
name|level
index|[
literal|2
index|]
operator|=
literal|0
expr_stmt|;
name|ctx
operator|->
name|path
operator|=
operator|&
name|path
expr_stmt|;
name|ctx
operator|->
name|log
operator|=
name|log
expr_stmt|;
name|ngx_collect_garbage
argument_list|(
name|ctx
argument_list|,
operator|&
name|path
operator|.
name|name
argument_list|,
literal|0
argument_list|)
expr_stmt|;
block|}
end_function

begin_function
DECL|function|ngx_collect_garbage (ngx_gc_t * ctx,ngx_str_t * dname,int level)
specifier|static
name|int
name|ngx_collect_garbage
parameter_list|(
name|ngx_gc_t
modifier|*
name|ctx
parameter_list|,
name|ngx_str_t
modifier|*
name|dname
parameter_list|,
name|int
name|level
parameter_list|)
block|{
name|int
name|nlen
decl_stmt|;
name|char
modifier|*
name|last
decl_stmt|;
name|ngx_str_t
name|fname
decl_stmt|;
name|ngx_dir_t
modifier|*
name|dir
decl_stmt|;
name|ngx_dirent_t
modifier|*
name|de
decl_stmt|;
name|ngx_file_info_t
name|fi
decl_stmt|;
name|fname
operator|.
name|len
operator|=
literal|0
expr_stmt|;
name|ngx_log_debug
argument_list|(
argument|ctx->log
argument_list|,
literal|"dir %s"
argument|_ dname->data
argument_list|)
empty_stmt|;
name|dir
operator|=
name|ngx_open_dir
argument_list|(
name|dname
operator|->
name|data
argument_list|)
expr_stmt|;
if|if
condition|(
name|dir
operator|==
name|NULL
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_ERR
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_open_dir_n
literal|" \"%s\" failed"
argument_list|,
name|dname
operator|->
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
for|for
control|(
init|;
condition|;
control|)
block|{
name|de
operator|=
name|ngx_read_dir
argument_list|(
name|dir
argument_list|)
expr_stmt|;
if|if
condition|(
name|de
operator|==
name|NULL
condition|)
block|{
if|if
condition|(
name|fname
operator|.
name|len
condition|)
block|{
name|ngx_free
argument_list|(
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
block|}
break|break;
block|}
name|ngx_log_debug
argument_list|(
argument|ctx->log
argument_list|,
literal|"file %s"
argument|_ de->d_name
argument_list|)
empty_stmt|;
ifdef|#
directive|ifdef
name|__FreeBSD__
name|nlen
operator|=
name|de
operator|->
name|d_namlen
expr_stmt|;
else|#
directive|else
name|nlen
operator|=
name|ngx_strlen
argument_list|(
name|de
operator|->
name|d_name
argument_list|)
expr_stmt|;
endif|#
directive|endif
if|if
condition|(
name|nlen
operator|==
literal|1
operator|&&
name|de
operator|->
name|d_name
index|[
literal|0
index|]
operator|==
literal|'.'
condition|)
block|{
continue|continue;
block|}
if|if
condition|(
name|nlen
operator|==
literal|2
operator|&&
name|de
operator|->
name|d_name
index|[
literal|0
index|]
operator|==
literal|'.'
operator|&&
name|de
operator|->
name|d_name
index|[
literal|1
index|]
operator|==
literal|'.'
condition|)
block|{
continue|continue;
block|}
if|if
condition|(
name|dname
operator|->
name|len
operator|+
literal|1
operator|+
name|nlen
operator|>
name|fname
operator|.
name|len
condition|)
block|{
if|if
condition|(
name|fname
operator|.
name|len
condition|)
block|{
name|ngx_free
argument_list|(
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
block|}
name|fname
operator|.
name|len
operator|=
name|dname
operator|->
name|len
operator|+
literal|1
operator|+
name|nlen
expr_stmt|;
if|if
condition|(
operator|!
operator|(
name|fname
operator|.
name|data
operator|=
name|ngx_alloc
argument_list|(
name|fname
operator|.
name|len
operator|+
literal|1
argument_list|,
name|ctx
operator|->
name|log
argument_list|)
operator|)
condition|)
block|{
return|return
name|NGX_ABORT
return|;
block|}
block|}
name|last
operator|=
name|ngx_cpymem
argument_list|(
name|fname
operator|.
name|data
argument_list|,
name|dname
operator|->
name|data
argument_list|,
name|dname
operator|->
name|len
argument_list|)
expr_stmt|;
operator|*
name|last
operator|++
operator|=
literal|'/'
expr_stmt|;
name|ngx_memcpy
argument_list|(
name|last
argument_list|,
name|de
operator|->
name|d_name
argument_list|,
name|nlen
operator|+
literal|1
argument_list|)
expr_stmt|;
name|ngx_log_debug
argument_list|(
argument|ctx->log
argument_list|,
literal|"de %s"
argument|_ fname.data
argument_list|)
empty_stmt|;
if|if
condition|(
name|ngx_file_type
argument_list|(
name|fname
operator|.
name|data
argument_list|,
operator|&
name|fi
argument_list|)
operator|==
name|NGX_FILE_ERROR
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_file_type_n
literal|" \"%s\" failed"
argument_list|,
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|ngx_is_dir
argument_list|(
operator|(
operator|&
name|fi
operator|)
argument_list|)
condition|)
block|{
name|ngx_log_debug
argument_list|(
argument|ctx->log
argument_list|,
literal|"enter %s"
argument|_ fname.data
argument_list|)
empty_stmt|;
if|if
condition|(
name|level
operator|==
operator|-
literal|1
comment|/* there can not be directory on the last level */
operator|||
name|level
operator|==
name|NGX_MAX_PATH_LEVEL
comment|/* an directory from the old path hierarchy */
operator|||
name|nlen
operator|!=
name|ctx
operator|->
name|path
operator|->
name|level
index|[
name|level
index|]
condition|)
block|{
if|if
condition|(
name|ngx_collect_garbage
argument_list|(
name|ctx
argument_list|,
operator|&
name|fname
argument_list|,
operator|-
literal|1
argument_list|)
operator|==
name|NGX_ABORT
condition|)
block|{
return|return
name|NGX_ABORT
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_NOTICE
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
literal|0
argument_list|,
literal|"delete old hierachy directory \"%s\""
argument_list|,
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
if|if
condition|(
name|ngx_delete_dir
argument_list|(
name|fname
operator|.
name|data
argument_list|)
operator|==
name|NGX_FILE_ERROR
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_delete_dir_n
literal|" \"%s\" failed"
argument_list|,
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|ctx
operator|->
name|deleted
operator|++
expr_stmt|;
name|ctx
operator|->
name|freed
operator|+=
name|ngx_file_size
argument_list|(
operator|(
operator|&
name|fi
operator|)
argument_list|)
expr_stmt|;
block|}
continue|continue;
block|}
if|if
condition|(
name|ngx_collect_garbage
argument_list|(
name|ctx
argument_list|,
operator|&
name|fname
argument_list|,
name|level
operator|+
literal|1
argument_list|)
operator|==
name|NGX_ABORT
condition|)
block|{
return|return
name|NGX_ABORT
return|;
block|}
block|}
if|else if
condition|(
name|ngx_is_file
argument_list|(
operator|(
operator|&
name|fi
operator|)
argument_list|)
condition|)
block|{
if|if
condition|(
name|level
operator|==
operator|-
literal|1
operator|||
operator|(
name|level
operator|<
name|NGX_MAX_PATH_LEVEL
operator|&&
name|ctx
operator|->
name|path
operator|->
name|level
index|[
name|level
index|]
operator|!=
literal|0
operator|)
condition|)
block|{
if|if
condition|(
name|ngx_delete_file
argument_list|(
name|fname
operator|.
name|data
argument_list|)
operator|==
name|NGX_FILE_ERROR
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_delete_file_n
literal|" \"%s\" failed"
argument_list|,
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|ctx
operator|->
name|deleted
operator|++
expr_stmt|;
name|ctx
operator|->
name|freed
operator|+=
name|ngx_file_size
argument_list|(
operator|(
operator|&
name|fi
operator|)
argument_list|)
expr_stmt|;
block|}
continue|continue;
block|}
if|if
condition|(
name|ctx
operator|->
name|handler
argument_list|(
name|ctx
argument_list|,
operator|&
name|fname
argument_list|,
operator|&
name|fi
argument_list|)
operator|==
name|NGX_ABORT
condition|)
block|{
return|return
name|NGX_ABORT
return|;
block|}
block|}
else|else
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
literal|"\"%s\" has unknown file type, deleting"
argument_list|,
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
if|if
condition|(
name|ngx_delete_file
argument_list|(
name|fname
operator|.
name|data
argument_list|)
operator|==
name|NGX_FILE_ERROR
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_delete_file_n
literal|" \"%s\" failed"
argument_list|,
name|fname
operator|.
name|data
argument_list|)
expr_stmt|;
block|}
else|else
block|{
name|ctx
operator|->
name|deleted
operator|++
expr_stmt|;
name|ctx
operator|->
name|freed
operator|+=
name|ngx_file_size
argument_list|(
operator|(
operator|&
name|fi
operator|)
argument_list|)
expr_stmt|;
block|}
block|}
block|}
return|return
name|NGX_OK
return|;
block|}
end_function

begin_function
DECL|function|ngx_garbage_collector_temp_handler (ngx_gc_t * ctx,ngx_str_t * name,ngx_file_info_t * fi)
name|int
name|ngx_garbage_collector_temp_handler
parameter_list|(
name|ngx_gc_t
modifier|*
name|ctx
parameter_list|,
name|ngx_str_t
modifier|*
name|name
parameter_list|,
name|ngx_file_info_t
modifier|*
name|fi
parameter_list|)
block|{
comment|/*      * we use mtime only and do not use atime because:      *    on NTFS access time has a resolution of 1 hour,      *    on NT FAT access time has a resolution of 1 day,      *    Unices have mount option "noatime"      */
if|if
condition|(
name|ngx_cached_time
operator|-
name|ngx_file_mtime
argument_list|(
name|fi
argument_list|)
operator|<
literal|3600
condition|)
block|{
return|return
name|NGX_OK
return|;
block|}
name|ngx_log_error
argument_list|(
name|NGX_LOG_NOTICE
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
literal|0
argument_list|,
literal|"delete stale temporary \"%s\""
argument_list|,
name|name
operator|->
name|data
argument_list|)
expr_stmt|;
if|if
condition|(
name|ngx_delete_file
argument_list|(
name|name
operator|->
name|data
argument_list|)
operator|==
name|NGX_FILE_ERROR
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_CRIT
argument_list|,
name|ctx
operator|->
name|log
argument_list|,
name|ngx_errno
argument_list|,
name|ngx_delete_file_n
literal|" \"%s\" failed"
argument_list|,
name|name
operator|->
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
name|ctx
operator|->
name|deleted
operator|++
expr_stmt|;
name|ctx
operator|->
name|freed
operator|+=
name|ngx_file_size
argument_list|(
name|fi
argument_list|)
expr_stmt|;
return|return
name|NGX_OK
return|;
block|}
end_function

end_unit

