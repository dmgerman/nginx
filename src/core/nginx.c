begin_unit|revision:1.0.0;language:C;cregit-version:0.0.1
begin_include
include|#
directive|include
file|<ngx_config.h>
end_include

begin_include
include|#
directive|include
file|<ngx_core.h>
end_include

begin_include
include|#
directive|include
file|<ngx_event.h>
end_include

begin_include
include|#
directive|include
file|<nginx.h>
end_include

begin_function_decl
specifier|static
name|int
name|ngx_open_listening_sockets
parameter_list|(
name|ngx_log_t
modifier|*
name|log
parameter_list|)
function_decl|;
end_function_decl

begin_decl_stmt
DECL|variable|ngx_conf_ctx
name|void
modifier|*
modifier|*
modifier|*
modifier|*
name|ngx_conf_ctx
decl_stmt|;
end_decl_stmt

begin_decl_stmt
DECL|variable|ngx_io
name|ngx_os_io_t
name|ngx_io
decl_stmt|;
end_decl_stmt

begin_decl_stmt
DECL|variable|ngx_max_module
name|int
name|ngx_max_module
decl_stmt|;
end_decl_stmt

begin_decl_stmt
DECL|variable|ctx_conf
name|void
modifier|*
name|ctx_conf
decl_stmt|;
end_decl_stmt

begin_decl_stmt
DECL|variable|ngx_connection_counter
name|int
name|ngx_connection_counter
decl_stmt|;
end_decl_stmt

begin_decl_stmt
DECL|variable|ngx_listening_sockets
name|ngx_array_t
name|ngx_listening_sockets
decl_stmt|;
end_decl_stmt

begin_if
if|#
directive|if
literal|0
end_if

begin_comment
unit|int main(int argc, char *const *argv) {     ngx_str_t     conf_file;     ngx_log_t    *log;     ngx_conf_t    conf;     ngx_cycle_t  *cycle;
comment|/* TODO */
end_comment

begin_comment
unit|ngx_max_sockets = -1;      log = ngx_log_init_errlog();      if (ngx_os_init(log) == NGX_ERROR) {         return 1;     }      ngx_max_module = 0;     for (i = 0; ngx_modules[i]; i++) {         ngx_modules[i]->index = ngx_max_module++;     }      cycle = ngx_init_cycle(NULL, log);     if (cycle == NULL) {         return 1;     }
comment|/* daemon */
end_comment

begin_comment
comment|/* life cycle */
end_comment

begin_comment
unit|for ( ;; ) {
comment|/* forks */
end_comment

begin_comment
unit|ngx_init_temp_number();
comment|/* threads */
end_comment

begin_comment
unit|for ( ;; ) {              worker(cycle->log);              new_cycle = ngx_init_cycle(cycle, cycle->log);             if (new_cycle) == NULL) {                 continue;             }              cycle = new_cycle;             break;         }     }      return 0; }   static int ngx_init_cycle(ngx_cycle_t *old_cycle, ngx_log_t *log) {     int               i, n;     ngx_conf_t        conf;     ngx_pool_t       *pool;     ngx_cycle_t      *cycle;     ngx_open_file_t  *file;     ngx_listening_t  *ls, *nls;       pool = ngx_create_pool(16 * 1024, log);     if (pool == NULL) {         return NULL;     }      cycle = ngx_pcalloc(pool, sizeof(ngx_cycle_t));     if (cycle == NULL) {         ngx_destroy_pool(pool);         return NULL;     }     cycle->pool = pool;      n = old_cycle ? old_cycle->open_files.nelts : 20;     cycle->open_files.elts = ngx_pcalloc(pool, n * sizeof(ngx_open_file_t));     if (cycle->open_files.elts == NULL) {         ngx_destroy_pool(pool);         return NULL;     }     cycle->open_files.nelts = 0;     cycle->open_files.size = sizeof(ngx_open_file_t);     cycle->open_files.nalloc = n;     cycle->open_files.pool = pool;      n = old_cycle ? old_cycle->listening.nelts : 10;     cycle->listening.elts = ngx_pcalloc(pool, n * sizeof(ngx_listening_t));     if (cycle->listening.elts == NULL) {         ngx_destroy_pool(pool);         return NULL;     }     cycle->listening.nelts = 0;     cycle->listening.size = sizeof(ngx_listening_t);     cycle->listening.nalloc = n;     cycle->listening.pool = pool;      cycle->conf_ctx = ngx_pcalloc(pool, ngx_max_module * sizeof(void *));     if (cycle->conf_ctx == NULL) {         ngx_destroy_pool(pool);         return NULL;     }      ngx_memzero(&conf, sizeof(ngx_conf_t));
comment|/* STUB: init array ? */
end_comment

begin_comment
unit|conf.args = ngx_create_array(pool, 10, sizeof(ngx_str_t));     if (conf.args == NULL) {         ngx_destroy_pool(pool);         return NULL;     }      conf.ctx = cycle->conf_ctx;     conf.cycle = cycle;
comment|/* STUB */
end_comment

begin_comment
unit|conf.pool = cycle->pool;     conf.log = log;     conf.module_type = NGX_CORE_MODULE;     conf.cmd_type = NGX_MAIN_CONF;      conf_file.len = sizeof(NGINX_CONF) - 1;     conf_file.data = NGINX_CONF;      if (ngx_conf_parse(&conf,&conf_file) != NGX_CONF_OK) {         ngx_destroy_pool(pool);         return NULL;     }      for (i = 0; ngx_modules[i]; i++) {         if (ngx_modules[i]->init_module) {             if (ngx_modules[i]->init_module(cycle, log) == NGX_ERROR)             {                 failed = 1;                 break;             }         }     }      if (!failed) {         file = cycle->open_files.elts;         for (i = 0; i< cycle->open_files.nelts; i++) {             if (file->name.data = NULL) {                 continue;             }              file->fd = ngx_open_file(file->name.data,                                      NGX_FILE_RDWR,                                      NGX_FILE_CREATE_OR_OPEN|NGX_FILE_APPEND);              if (file->fd == NGX_INVALID_FILE) {                 ngx_log_error(NGX_LOG_EMERG, log, ngx_errno,                               ngx_open_file_n " \"%s\" failed",                               file->name.data);                 failed = 1;                 break;             }
comment|/* TODO: Win32 append */
end_comment

begin_comment
unit|}     }      if (!failed) {         ls = old_cycle->listening.elts;         for (i = 0; i< old_cycle->listening.nelts; i++) {             ls[i].remain = 0;         }          nls = cycle->listening.elts;         for (n = 0; n< cycle->listening.nelts; n++) {             for (i = 0; i< old_cycle->listening.nelts; i++) {                 if (ngx_memcmp(nls[n].sockaddr,                                ls[i].sockaddr, ls[i].socklen) == 0)                 {                     nls[n].fd = ls[i].fd;                     ls[i].remain = 1;                     break;                 }             }              if (nls[n].fd == -1) {                 nls[n].new = 1;             }         }          if (ngx_open_listening_sockets(new_cycle) == NGX_ERROR) {             failed = 1;         }     }      if (failed) {
comment|/* rollback the new cycle configuration */
end_comment

begin_comment
unit|for (i = 0; ngx_modules[i]; i++) {             if (ngx_modules[i]->rollback_module) {                 ngx_modules[i]->rollback_module(cycle);             }         }          file = cycle->open_files.elts;         for (i = 0; i< cycle->open_files.nelts; i++) {             if (file->fd == NGX_INVALID_FILE) {                 continue;             }              if (ngx_close_file(file.fd) == NGX_FILE_ERROR) {                 ngx_log_error(NGX_LOG_EMERG, log, ngx_errno,                               ngx_close_file_n " \"%s\" failed",                               file->name.data);             }         }          ls[i] = cycle->listening.elts;         for (i = 0; i< cycle->listening.nelts; i++) {             if (ls[i].new&& ls[i].fd == -1) {                 continue;             }              if (ngx_close_socket(ls[i].fd) == -1)                 ngx_log_error(NGX_LOG_EMERG, log, ngx_socket_errno,                               ngx_close_socket_n " %s failed",                               ls[i].addr_text.data);             }         }          ngx_destroy_pool(pool);         return NULL;      } else {
comment|/* commit the new cycle configuration */
end_comment

begin_else
unit|for (i = 0; ngx_modules[i]; i++) {             if (ngx_modules[i]->commit_module) {                 ngx_modules[i]->commit_module(cycle);             }         }     }      ls = old_cycle->listening.elts;     for (i = 0; i< old_cycle->listening.nelts; i++) {         if (ls[i].remain) {             continue;         }          if (ngx_close_socket(ls[i].fd) == -1)             ngx_log_error(NGX_LOG_EMERG, log, ngx_socket_errno,                           ngx_close_socket_n " %s failed",                           ls[i].addr_text.data);         }     }      file = old_cycle->open_files.elts;     for (i = 0; i< cycle->old_open_files.nelts; i++) {         if (file->fd == NGX_INVALID_FILE) {             continue;         }          if (ngx_close_file(file.fd) == NGX_FILE_ERROR) {             ngx_log_error(NGX_LOG_EMERG, log, ngx_errno,                           ngx_close_file_n " \"%s\" failed",                           file->name.data);         }     }      new_cycle->log = ???;     pool->log = ???;      ngx_destroy_pool(old_cycle->pool);      return cycle; }
else|#
directive|else
end_else

begin_function
DECL|function|main (int argc,char * const * argv)
name|int
name|main
parameter_list|(
name|int
name|argc
parameter_list|,
name|char
modifier|*
specifier|const
modifier|*
name|argv
parameter_list|)
block|{
name|int
name|i
decl_stmt|;
name|ngx_str_t
name|conf_file
decl_stmt|;
name|ngx_log_t
modifier|*
name|log
decl_stmt|;
name|ngx_pool_t
modifier|*
name|pool
decl_stmt|,
modifier|*
name|old_pool
decl_stmt|;
name|ngx_conf_t
name|conf
decl_stmt|;
name|ngx_max_sockets
operator|=
operator|-
literal|1
expr_stmt|;
name|log
operator|=
name|ngx_log_init_errlog
argument_list|()
expr_stmt|;
if|if
condition|(
name|ngx_os_init
argument_list|(
name|log
argument_list|)
operator|==
name|NGX_ERROR
condition|)
block|{
return|return
literal|1
return|;
block|}
name|ngx_max_module
operator|=
literal|0
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|ngx_modules
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
block|{
name|ngx_modules
index|[
name|i
index|]
operator|->
name|index
operator|=
name|ngx_max_module
operator|++
expr_stmt|;
block|}
comment|/* life cycle */
block|{
name|old_pool
operator|=
name|pool
expr_stmt|;
name|pool
operator|=
name|ngx_create_pool
argument_list|(
literal|16
operator|*
literal|1024
argument_list|,
name|log
argument_list|)
expr_stmt|;
name|ngx_init_array
argument_list|(
name|ngx_listening_sockets
argument_list|,
name|pool
argument_list|,
literal|10
argument_list|,
sizeof|sizeof
argument_list|(
name|ngx_listening_t
argument_list|)
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|ngx_memzero
argument_list|(
operator|&
name|conf
argument_list|,
sizeof|sizeof
argument_list|(
name|ngx_conf_t
argument_list|)
argument_list|)
expr_stmt|;
name|ngx_test_null
argument_list|(
name|conf
operator|.
name|args
argument_list|,
name|ngx_create_array
argument_list|(
name|pool
argument_list|,
literal|10
argument_list|,
sizeof|sizeof
argument_list|(
name|ngx_str_t
argument_list|)
argument_list|)
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|ngx_test_null
argument_list|(
name|ngx_conf_ctx
argument_list|,
name|ngx_pcalloc
argument_list|(
name|pool
argument_list|,
name|ngx_max_module
operator|*
sizeof|sizeof
argument_list|(
name|void
operator|*
argument_list|)
argument_list|)
argument_list|,
literal|1
argument_list|)
expr_stmt|;
name|conf
operator|.
name|ctx
operator|=
name|ngx_conf_ctx
expr_stmt|;
name|conf
operator|.
name|pool
operator|=
name|pool
expr_stmt|;
name|conf
operator|.
name|log
operator|=
name|log
expr_stmt|;
name|conf
operator|.
name|module_type
operator|=
name|NGX_CORE_MODULE
expr_stmt|;
name|conf
operator|.
name|cmd_type
operator|=
name|NGX_MAIN_CONF
expr_stmt|;
name|conf_file
operator|.
name|len
operator|=
sizeof|sizeof
argument_list|(
name|NGINX_CONF
argument_list|)
operator|-
literal|1
expr_stmt|;
name|conf_file
operator|.
name|data
operator|=
name|NGINX_CONF
expr_stmt|;
if|if
condition|(
name|ngx_conf_parse
argument_list|(
operator|&
name|conf
argument_list|,
operator|&
name|conf_file
argument_list|)
operator|!=
name|NGX_CONF_OK
condition|)
block|{
return|return
literal|1
return|;
block|}
if|#
directive|if
literal|0
block_content|log = (ngx_log_t *) ngx_get_conf(ngx_errlog_module);
comment|/* STUB */
block_content|log->log_level = NGX_LOG_DEBUG;
endif|#
directive|endif
name|ngx_init_temp_number
argument_list|()
expr_stmt|;
name|ngx_io
operator|=
name|ngx_os_io
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|ngx_modules
index|[
name|i
index|]
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|ngx_modules
index|[
name|i
index|]
operator|->
name|init_module
condition|)
block|{
if|if
condition|(
name|ngx_modules
index|[
name|i
index|]
operator|->
name|init_module
argument_list|(
name|pool
argument_list|)
operator|==
name|NGX_ERROR
condition|)
block|{
return|return
literal|1
return|;
block|}
block|}
block|}
if|if
condition|(
name|ngx_open_listening_sockets
argument_list|(
name|log
argument_list|)
operator|==
name|NGX_ERROR
condition|)
block|{
return|return
literal|1
return|;
block|}
comment|/* TODO: daemon, once only */
comment|/* TODO: fork */
name|ngx_pre_thread
argument_list|(
operator|&
name|ngx_listening_sockets
argument_list|,
name|pool
argument_list|,
name|log
argument_list|)
expr_stmt|;
comment|/* TODO: threads */
comment|/* STUB */
name|ngx_worker
argument_list|(
name|log
argument_list|)
expr_stmt|;
block|}
return|return
literal|0
return|;
block|}
end_function

begin_endif
endif|#
directive|endif
end_endif

begin_function
DECL|function|ngx_open_listening_sockets (ngx_log_t * log)
specifier|static
name|int
name|ngx_open_listening_sockets
parameter_list|(
name|ngx_log_t
modifier|*
name|log
parameter_list|)
block|{
name|int
name|times
decl_stmt|,
name|failed
decl_stmt|,
name|reuseaddr
decl_stmt|,
name|i
decl_stmt|;
name|ngx_err_t
name|err
decl_stmt|;
name|ngx_socket_t
name|s
decl_stmt|;
name|ngx_listening_t
modifier|*
name|ls
decl_stmt|;
name|reuseaddr
operator|=
literal|1
expr_stmt|;
for|for
control|(
name|times
operator|=
literal|10
init|;
name|times
condition|;
name|times
operator|--
control|)
block|{
name|failed
operator|=
literal|0
expr_stmt|;
comment|/* for each listening socket */
name|ls
operator|=
name|ngx_listening_sockets
operator|.
name|elts
expr_stmt|;
for|for
control|(
name|i
operator|=
literal|0
init|;
name|i
operator|<
name|ngx_listening_sockets
operator|.
name|nelts
condition|;
name|i
operator|++
control|)
block|{
if|if
condition|(
name|ls
index|[
name|i
index|]
operator|.
name|bound
condition|)
continue|continue;
if|if
condition|(
name|ls
index|[
name|i
index|]
operator|.
name|inherited
condition|)
block|{
comment|/* TODO: close on exit */
comment|/* TODO: nonblocking */
comment|/* TODO: deferred accept */
name|ls
index|[
name|i
index|]
operator|.
name|bound
operator|=
literal|1
expr_stmt|;
continue|continue;
block|}
name|s
operator|=
name|ngx_socket
argument_list|(
name|ls
index|[
name|i
index|]
operator|.
name|family
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|type
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|protocol
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|flags
argument_list|)
expr_stmt|;
if|if
condition|(
name|s
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|log
argument_list|,
name|ngx_socket_errno
argument_list|,
name|ngx_socket_n
literal|" %s falied"
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|addr_text
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
if|#
directive|if
operator|(
name|WIN32
operator|)
comment|/*              * Winsock assignes a socket number divisible by 4              * so to find a connection we divide a socket number by 4.              */
if|if
condition|(
name|s
operator|%
literal|4
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|ls
operator|->
name|log
argument_list|,
literal|0
argument_list|,
name|ngx_socket_n
literal|" created socket %d"
argument_list|,
name|s
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
endif|#
directive|endif
if|if
condition|(
name|setsockopt
argument_list|(
name|s
argument_list|,
name|SOL_SOCKET
argument_list|,
name|SO_REUSEADDR
argument_list|,
operator|(
specifier|const
name|void
operator|*
operator|)
operator|&
name|reuseaddr
argument_list|,
sizeof|sizeof
argument_list|(
name|int
argument_list|)
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|log
argument_list|,
name|ngx_socket_errno
argument_list|,
literal|"setsockopt(SO_REUSEADDR) %s failed"
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|addr_text
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
comment|/* TODO: close on exit */
if|if
condition|(
name|ls
index|[
name|i
index|]
operator|.
name|nonblocking
condition|)
block|{
if|if
condition|(
name|ngx_nonblocking
argument_list|(
name|s
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|log
argument_list|,
name|ngx_socket_errno
argument_list|,
name|ngx_nonblocking_n
literal|" %s failed"
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|addr_text
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
block|}
if|if
condition|(
name|bind
argument_list|(
name|s
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|sockaddr
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|socklen
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|err
operator|=
name|ngx_socket_errno
expr_stmt|;
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|log
argument_list|,
name|err
argument_list|,
literal|"bind() to %s failed"
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|addr_text
operator|.
name|data
argument_list|)
expr_stmt|;
if|if
condition|(
name|err
operator|!=
name|NGX_EADDRINUSE
condition|)
return|return
name|NGX_ERROR
return|;
if|if
condition|(
name|ngx_close_socket
argument_list|(
name|s
argument_list|)
operator|==
operator|-
literal|1
condition|)
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|log
argument_list|,
name|ngx_socket_errno
argument_list|,
name|ngx_close_socket_n
literal|" %s failed"
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|addr_text
operator|.
name|data
argument_list|)
expr_stmt|;
name|failed
operator|=
literal|1
expr_stmt|;
continue|continue;
block|}
if|if
condition|(
name|listen
argument_list|(
name|s
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|backlog
argument_list|)
operator|==
operator|-
literal|1
condition|)
block|{
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|log
argument_list|,
name|ngx_socket_errno
argument_list|,
literal|"listen() to %s failed"
argument_list|,
name|ls
index|[
name|i
index|]
operator|.
name|addr_text
operator|.
name|data
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
comment|/* TODO: deferred accept */
name|ls
index|[
name|i
index|]
operator|.
name|fd
operator|=
name|s
expr_stmt|;
name|ls
index|[
name|i
index|]
operator|.
name|bound
operator|=
literal|1
expr_stmt|;
block|}
if|if
condition|(
operator|!
name|failed
condition|)
break|break;
name|ngx_log_error
argument_list|(
name|NGX_LOG_NOTICE
argument_list|,
name|log
argument_list|,
literal|0
argument_list|,
literal|"try again to bind() after 500ms"
argument_list|)
expr_stmt|;
name|ngx_msleep
argument_list|(
literal|500
argument_list|)
expr_stmt|;
block|}
if|if
condition|(
name|failed
condition|)
block|{
comment|/* TODO: configurable */
name|ngx_log_error
argument_list|(
name|NGX_LOG_EMERG
argument_list|,
name|log
argument_list|,
literal|0
argument_list|,
literal|"can not bind(), exiting"
argument_list|)
expr_stmt|;
return|return
name|NGX_ERROR
return|;
block|}
return|return
name|NGX_OK
return|;
block|}
end_function

end_unit

